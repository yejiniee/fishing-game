# Supabase 전역 랭킹 설정 (PRD §5.4)
# 이 파일을 .env 로 복사한 뒤 실제 값을 채운다. .env 는 커밋하지 않는다.
# 값은 Supabase 대시보드 > Project Settings > API 에서 확인한다.
# anon key 는 클라이언트에 노출되어도 되는 공개 키다(실제 방어선은 RLS).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
