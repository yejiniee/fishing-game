# 방어잡기 — 랭킹 & 공유 구현 체크리스트

PRD의 §3.8 랭킹, §3.9 공유, §5.4~5.5 기술 설계를 구현하기 위한 작업 목록.
각 항목은 별도 feature 브랜치에서 하나씩 진행한다.

- [x] PRD 문서 업데이트 — 랭킹·공유 기능을 PRD(§2, §3.8, §3.9, §4, §5, §6)에 반영
- [ ] Supabase 셋업 — scores 테이블·인덱스 SQL, RLS 정책, `.env.example`, 클라이언트 설정
- [ ] 기기 ID 유틸 — localStorage에 UUID를 생성·보관하는 `lib/deviceId.ts`
- [ ] 랭킹 API — `submitScore`(device_id upsert) / `fetchTopScores` + 타입 정의
- [ ] 닉네임 입력 UI — 게임오버 시 최초 1회 입력, 2~8자 검증, 변경 가능
- [ ] 랭킹 보드 UI — TOP N + 내 순위 하이라이트, 로딩/에러/오프라인 처리
- [ ] 결과 공유 — 결과 카드 이미지 생성 + Web Share API / 다운로드 fallback
- [ ] 게임오버 오버레이 통합 — 기존 화면에 랭킹·공유 배치
